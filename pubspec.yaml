name: show_me_the_camera
description: A camera show package
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+5
  flutter_bloc: ^8.0.0
  freezed_annotation: ^2.2.0
  freezed: 2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ">=2.3.0 <4.0.0"

flutter:
